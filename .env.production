# Production Environment Configuration
# Copy this file to .env and fill in the values

# API Key for sensor authentication
# Generate a strong random key: openssl rand -hex 32
SENSOR_API_KEY=your-secure-api-key-here

# Port to expose (default: 3000)
# If using a reverse proxy, you can keep this as 3000
PORT=3000

# SSL Certificate paths (relative to docker-compose.yml)
# These will be mounted as volumes in the container
# SSL_CERT_PATH and SSL_KEY_PATH are set in docker-compose.yml
